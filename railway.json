{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python seed_db.py && gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --workers 1 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
